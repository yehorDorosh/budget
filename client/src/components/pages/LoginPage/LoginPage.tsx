import { Fragment } from 'react'

import LoginForm from '../../auth/LoginForm/LoginForm'

const LoginPage = () => {
  return (
    <Fragment>
      <h1>Login</h1>
      <LoginForm />
    </Fragment>
  )
}

export default LoginPage
